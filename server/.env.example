# ====== BASE ======
NODE_ENV=development
PORT=3000

# ====== DATABASE ======
DATABASE_URL=postgresql://user:password@localhost:5432/academyhub

# ====== JWT ======
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# ====== IMPORT - SFTP Configuration ======
# Configuración para descargar archivos CSV de SAGE desde un servidor SFTP
# Estos parámetros permiten automatizar la importación sin tener que subir
# los archivos manualmente a través de la interfaz web.

# Servidor SFTP
SFTP_SAGE_HOST=sftp.example.com

# Puerto SFTP (por defecto 22 si se omite)
SFTP_SAGE_PORT=22

# Usuario para autenticarse en el servidor SFTP
SFTP_SAGE_USER=usuario_sftp

# Contraseña para autenticarse en el servidor SFTP
SFTP_SAGE_PASSWORD=contraseña_segura

# Ruta completa del archivo CSV en el servidor SFTP
# Ejemplo: /uploads/sage/usuarios.csv
SFTP_SAGE_PATH=/directorio/donde/esta/el/archivo.csv

# ====== IMPORT - Velneo CSV Writer ======
# Controla si el importer de Velneo escribe archivo de errores (bad-rows.csv)
# Por defecto deshabilitado para evitar crear archivos de depuración
# Establecer a "true" solo cuando se necesite diagnóstico detallado
IMPORT_VELNEO_WRITE_BAD_ROWS=false

# ====== SCHEDULER INTERNO ======
# Scheduler interno con setInterval (recomendado para apps de 1 instancia)
# Activar scheduler interno para tareas programadas automáticas
ENABLE_CRON_SCHEDULER=false

# --- Tarea: Importación SAGE ---
# Habilitar importación automática de SAGE desde SFTP
SAGE_IMPORT_ENABLED=true

# Expresión cron para programar la importación
# Formato: "minuto hora día mes día_semana"
# Ejemplos: 
#   "0 2 * * *" = 2:00 AM todos los días (default)
#   "0 */6 * * *" = cada 6 horas
#   "0 3 * * 1-5" = 3:00 AM de lunes a viernes
SAGE_IMPORT_CRON=0 2 * * *

# Ejecutar importación inmediatamente al arrancar la app (útil para testing)
SAGE_IMPORT_RUN_ON_STARTUP=false

# --- Futuras tareas programadas ---
# Puedes añadir más tareas como:
# CLEANUP_JOBS_ENABLED=true
# CLEANUP_JOBS_CRON=0 3 * * *  # 3:00 AM todos los días
# BACKUP_DB_ENABLED=true
# BACKUP_DB_CRON=0 1 * * *  # 1:00 AM todos los días

# Timezone del scheduler (por defecto UTC)
# Ejemplos: America/New_York, Europe/Madrid, Asia/Tokyo
SCHEDULER_TIMEZONE=UTC

# ====== OPTIONAL: MAIL CONFIG (if needed) ======
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USER=your_email@gmail.com
# MAIL_PASS=your_app_password
# MAIL_FROM=noreply@academyhub.local
